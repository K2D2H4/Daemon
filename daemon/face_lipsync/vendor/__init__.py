"""Third-party source vendored verbatim. Nothing in here is ours to edit.

Anything under this directory is a byte-for-byte copy of an upstream file, kept
because the upstream is not installable: patching it locally would make the copy
unverifiable against its source, which is the one property that makes vendoring
defensible. Adaptations belong in the modules that call in here.
"""
